pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // ESTA ES LA LÍNEA QUE FALTABA PARA LOS GRÁFICOS:
        maven { url 'https://jitpack.io' }
    }
}

rootProject.name = "VoiceFinance"
include(":app")